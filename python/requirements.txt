# Pinned package versions for the *Essential Considerations for Modeling Epidemics*
# companion repository. These versions are tested in CI on Python 3.10, 3.11,
# and 3.12.
#
# Bumping any version requires:
#   1. Verify all notebooks still execute (CI handles this automatically)
#   2. Verify all figures regenerate byte-identically (CI handles this)
#   3. Update CHANGELOG.md with a brief note about why the bump was made

# Numerical core
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3

# Plotting
matplotlib==3.9.2

# Notebook infrastructure
jupyter==1.1.1
ipykernel==6.29.5
notebook==7.2.2

# Parameter fitting and sensitivity
lmfit==1.3.2
SALib==1.5.1
pyDOE3==1.0.4

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# Code quality (used in CI)
ruff==0.7.1

# Development utilities
ipywidgets==8.1.5
